name: PR Check 96

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-96:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 96 passed"
