name: PR Check 95

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-95:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 95 passed"
