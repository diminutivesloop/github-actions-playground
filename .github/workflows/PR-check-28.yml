name: PR Check 28

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-28:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 28 passed"
