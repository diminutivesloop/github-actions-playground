name: PR Check 60

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-60:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 60 passed"
