name: PR Check 45

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-45:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 45 passed"
