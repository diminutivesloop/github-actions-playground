name: PR Check 29

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-29:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 29 passed"
