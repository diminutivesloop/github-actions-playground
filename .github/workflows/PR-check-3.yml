name: PR Check 3

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-3:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 3 passed"
