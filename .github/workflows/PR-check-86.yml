name: PR Check 86

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-86:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 86 passed"
