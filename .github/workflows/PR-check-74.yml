name: PR Check 74

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-74:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 74 passed"
