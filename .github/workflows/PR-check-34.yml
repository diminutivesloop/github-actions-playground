name: PR Check 34

on:
  push:
    branches: [ too-many-PR-checks ]
  pull_request:
    branches: [ too-many-PR-checks ]

  workflow_dispatch:

jobs:
  check-34:
    runs-on: ubuntu-latest

    steps:
      - run: echo "check 34 passed"
